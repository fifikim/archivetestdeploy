<body class="bg-light">
  <header>
    <%= render 'layouts/navbar' %>
  </header>
  <main>

    <div class="container text-center">
      <div>
        <span class="cripping-header">tagged_with</span>
        <span class="cripping-header2">tagged_with</span>
      </div>
      <div>
        <span class="cripping-header">"<%= @tag.name %>"</span>
        <span class="cripping-header2">"<%= @tag.name %>"</span>
      </div>
      <div class="mt-5">
        <div class="upload-gallery justify-content-around my-5 mx-auto">
          <% @tag.uploads.each do |upload| %>
            <% if upload.id != 42 && upload.id != 38 %>
              <div id="uploads">
                <%= render upload %>
              </div>
            <% end %>
          <% end %>
        </div>
        
      </div>
    </div>
  </main>
  <footer class="footer">©2022 CRIPPING_CG</footer>
</body>